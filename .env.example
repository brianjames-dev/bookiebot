# Discord
DISCORD_TOKEN=your-discord-bot-token
CHANNEL_NAME=your-discord-channel-name
# Optional: set CHANNEL_ID to enforce by ID instead of name
CHANNEL_ID=your-discord-channel-id
# Comma-separated Discord user IDs allowed to run /debug commands (e.g., 123,456)
DEBUG_ADMINS=

# Optional runtime metadata
BOT_ENV=dev
# If your platform injects commit info (e.g., RAILWAY_GIT_COMMIT_SHA), the bot will show it in /debug_status.
# You can also set GIT_SHA manually if your platform doesn’t provide it.
GIT_SHA=

# OpenAI
OPENAI_API_KEY=your-openai-api-key

# Google Sheets
# Supply the full JSON for your service account; keep it quoted if it contains whitespace/newlines.
EXPENSE_SHEET_KEY=your-expense-sheet-key
INCOME_SHEET_KEY=your-income-sheet-key
GOOGLE_SERVICE_ACCOUNT_JSON={"type": "service_account", "project_id": "...", "private_key": "...", "client_email": "..."}

# Timezone (optional override)
TZ=America/Los_Angeles
