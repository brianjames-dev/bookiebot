[pytest]
asyncio_mode = strict
markers =
    asyncio: mark a test as async.
    integration: exercises optional runtime dependencies such as Kaleido.
filterwarnings =
    ignore::DeprecationWarning:discord.player
